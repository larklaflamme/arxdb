# ArxDB — full runtime dependency set (pinned to the reference machine).
#
# The pyproject.toml `dependencies` list only the *core* deps (blake3, cbor2,
# cryptography). The verification checkers (sympy, mpmath, z3-solver) and the
# gRPC client (grpcio, protobuf) are additional runtime deps documented in
# SETUP.md. This file consolidates the complete set for the Docker image and
# for anyone who wants a one-shot `pip install -r requirements.txt`.

# Core (content addressing, serialization, signing)
blake3==1.0.9
cbor2==6.1.4
cryptography==50.0.1

# Verification checkers (κ/ELENCHUS)
sympy==1.14.0
mpmath==1.3.0
z3-solver==5.1.0.0

# gRPC client (Go storage backend)
grpcio==1.83.0
protobuf==7.36.0
